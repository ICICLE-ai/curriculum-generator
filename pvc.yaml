apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sailab-data-pvc
  namespace: sailab
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: rook-ceph-block
  resources:
    requests:
      storage: 50Gi
